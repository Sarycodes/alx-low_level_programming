#include "main.h"

/**
 *abs - computes the absolute value of an integer
 *@n: integer number
 *Return: Always 0 success
 */

int _abs(int n)
{
	int abs_value;

	if (n < 0)
	{
		abs_value = n * -1;
	}
}
